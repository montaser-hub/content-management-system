# Used by docker-compose.yml when running the whole stack (db + api) in one
# command. For plain local development (npm run start:dev against just the
# `db` service) the values in server/.env are what actually get used instead.
JWT_ACCESS_SECRET="replace-with-a-generated-secret"
CORS_ORIGIN="http://localhost:5173"
