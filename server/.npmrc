# npm 11+ blocks install/postinstall scripts from new dependencies by default
# (see: npm install-scripts ls) — this already stops swagger-ui-dist's bundled
# @scarf/scarf telemetry script from ever running. Nothing to configure here;
# this file exists so the reasoning is on record rather than assumed.
